name: CI

on:
  push:
    branches:
      - main
      - support/*
  pull_request:
  workflow_dispatch:

jobs:
  php:
    name: PHP
    uses: Icinga/github-actions/.github/workflows/php.yml@main
    with:
      php-extensions: pdo, pdo-sqlite
      databases: true
